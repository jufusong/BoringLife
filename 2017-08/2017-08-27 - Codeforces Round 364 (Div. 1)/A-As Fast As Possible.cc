#include <bits/stdc++.h>

using namespace std;

int main(int argc, char *argv[]) {
  vector<int> c = {1, 2, 3};
  vector<int> b;
  c.push_back(4);
  for (auto x : c) {
    cout << x << endl;
  }
  return 0;
}
